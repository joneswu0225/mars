<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jones.mars.repository.BlockMapper" >
    <insert id="insert" parameterType="Block" useGeneratedKeys="true" keyProperty="id">
        insert into block(`name`, `enterprise_id`, `create_time`, `update_time`, `operator_id`, `detail`, `plateform_flg`)
        values(#{name}, #{enterpriseId}, now(), now(), #{operatorId}, #{detail}, #{plateformFlg})
    </insert>
    <insert id="update" parameterType="Block" >
        update block
        set name = #{name}, detail = #{detail}
        <if test="enterpriseId != null">
            , enterprise_id = #{enterpriseId}
        </if>
        <if test="staus != null">
            , staus = #{staus}
        </if>
        , and operator_id = #{operatorId}
        ,update_time = now()
        where id=#{id}
    </insert>
    <select id="findList" resultType="Block" parameterType="Query">
        <include refid="com.jones.mars.repository.BaseMapper.startPage" />
        select * from block
        where delete_flg=0
        <if test="query != null and query.name != null and query.name !=''">
            and name like concat('%',#{query.name},'%')
        </if>
        <if test="query != null and query.plateformFlg != null">
            and plateform_flg=#{query.plateformFlg}
        </if>
        <if test="query != null and query.status != null">
            and status=#{query.status}
        </if>
        <if test="query != null and query.enterpriseId != null">
            and enterprise_id=#{query.enterpriseId}
        </if>
        order by id desc
        <include refid="com.jones.mars.repository.BaseMapper.endPage" />
    </select>
    <select id="findCount" resultType="java.lang.Integer" parameterType="Query">
        select count(*) from block
        where delete_flg=0
        <if test="query != null and query.name != null and query.name !=''">
            and name like concat('%',#{query.name},'%')
        </if>
        <if test="query != null and query.plateformFlg ! = null">
            and plateform_flg=#{query.plateformFlg}
        </if>
        <if test="query != null and query.status != null">
            and status=#{query.status}
        </if>
        <if test="query != null and query.enterpriseId != null">
            and enterprise_id=#{query.enterpriseId}
        </if>
    </select>
    <select id="findAllName" resultType="java.lang.Object" parameterType="Query">
        select id, name from block
        where delete_flg=0
        <if test="query != null and query.staus != null">
            and name like concat('%',#{query.name},'%')
        </if>
        <if test="query != null and query.plateformFlg != null">
            and plateform_flg=#{query.plateformFlg}
        </if>
        <if test="query != null and query.enterpriseId != null">
            and enterprise_id=#{query.enterpriseId}
        </if>
        order by id
    </select>
    <select id="findAll" resultType="Block">
        select * from block where delete_flg=0 order by id desc
    </select>
    <select id="findOne" resultType="Block">
        select * from block where id=#{id}
    </select>
    <update id="delete">
        update block set delete_flg=1 where id=#{id}
    </update>
</mapper>