<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jones.mars.repository.CompanyJoinMapper" >
    <insert id="insert" parameterType="CompanyJoinParam" useGeneratedKeys="true" keyProperty="id">
        insert into company_join(`company_name`, `contactor`, `title`, `email`, `mobile`, `create_time`, `update_time`)
        values(#{companyName}, #{contactor}, #{title}, #{email}, #{mobile}, now(), now())
    </insert>
    <insert id="update" parameterType="CompanyJoin">
        update company_join
        set status = #{status}, remark=#{remark} ,update_time = now()
        where id=#{id}
    </insert>
    <select id="findList" resultType="CompanyJoin" parameterType="Query">
        <include refid="com.jones.mars.repository.BaseMapper.startPage" />
        select * from company_join
        where delete_flg=0
        order by id desc
        <include refid="com.jones.mars.repository.BaseMapper.endPage" />
    </select>
    <select id="findCount" resultType="java.lang.Integer" parameterType="Query">
        select count(*) from company_join
        where delete_flg=0
    </select>
    <select id="findAll" resultType="CompanyJoin">
        select * from company_join where delete_flg=0 order by id desc
    </select>
    <select id="findOne" resultType="CompanyJoin">
        select * from company_join where delete_flg=0 id=#{id}
    </select>
    <update id="delete">
        update company_join set delete_flg=1 where id=#{id}
    </update>
</mapper>