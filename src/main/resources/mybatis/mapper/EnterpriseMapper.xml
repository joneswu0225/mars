<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jones.mars.repository.EnterpriseMapper" >
    <insert id="insert" parameterType="Enterprise">
        insert into enterprise(`name`, `detail`, `manager_id`, `base_path`, `create_time`, `update_time`)
        values(#{name}, #{detail}, #{managerId}, #{basePath}, now(), now())
    </insert>
    <insert id="update" parameterType="Enterprise">
        update enterprise
        set name = #{name}
        <if test="detail != null and detail != ''">
            , detail = #{detail}
        </if>
        <if test="managerId != null">
            , manager_id = #{managerId}
        </if>
        <if test="base_path != null">
            , base_path = #{basePath}
        </if>
        <if test="level != null">
            , level = #{level}
        </if>
        ,update_time = now()
        where id=#{id}
    </insert>
    <select id="findList" resultType="Enterprise" parameterType="Query">
        <include refid="com.jones.mars.repository.BaseMapper.startPage" />
        select d.*, p.name as managerName from enterprise d left join user_profile p on d.manager_id = p.user_id
        where 1=1
        <if test="query != null and query.name != null and query.name !=''">
            and d.name like concat('%',#{query.name},'%')
        </if>
        <if test="query != null and query.plateformFlg != null">
            and d.plateform_flg = #{query.plateformFlg}
        </if>
        order by d.id desc
        <include refid="com.jones.mars.repository.BaseMapper.endPage" />
    </select>
    <select id="findCount" resultType="java.lang.Integer" parameterType="Query">
        select count(*) from enterprise d
        where 1=1
        <if test="query != null and query.name != null and query.name !=''">
            and d.name like concat('%',#{query.name},'%')
        </if>
        <if test="query != null and query.plateformFlg != null">
            and d.plateform_flg = #{query.plateformFlg}
        </if>java.lang.
    </select>
    <select id="findAllName" resultType="Enterprise" parameterType="Query">
        select id, name from enterprise d
        where 1=1
        <if test="query != null and query.name != null and query.name !=''">
            and d.name like concat('%',#{query.name},'%')
        </if>
        <if test="query != null and query.plateformFlg != null">
            and d.plateform_flg = #{query.plateformFlg}
        </if>
        order by id desc
    </select>
    <select id="findAll" resultType="Enterprise">
        select * from enterprise order by id desc
    </select>
    <select id="findOne" resultType="Enterprise">
        select d.*, p.name as managerName from enterprise d left join user_profile p on d.manager_id = p.user_id
        where id=#{id}
    </select>
    <update id="delete">
        update enterprise set delete_flg=1  where id=#{id}
    </update>
</mapper>