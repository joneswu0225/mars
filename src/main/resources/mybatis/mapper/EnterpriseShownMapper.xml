<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jones.mars.repository.EnterpriseShownMapper" >
    <insert id="insert" parameterType="EnterpriseShown">
        insert into enterprise_shown(`name`, `image_url`, `enterprise_id`, `create_time`, `update_time`)
        values(#{name}, #{imageUrl}, #{enterpriseId}, now(), now())
    </insert>
    <insert id="update" parameterType="EnterpriseShown">
        update enterprise_shown
        set name = #{name}
        <if test="imageUrl != null and imageUrl != ''">
            , image_url = #{imageUrl}
        </if>
        <if test="enterpriseId != null">
            , enterprise_id = #{enterpriseId}
        </if>
        ,update_time = now()
        where id=#{id}
    </insert>
    <select id="findList" resultType="EnterpriseShown" parameterType="Query">
        <include refid="com.jones.mars.repository.BaseMapper.startPage" />
        select * from enterprise_shown
        where delete_flg=0
        order by id desc
        <include refid="com.jones.mars.repository.BaseMapper.endPage" />
    </select>
    <select id="findCount" resultType="java.lang.Integer" parameterType="Query">
        select count(*) from enterprise_shown
        where delete_flg=0
        order by id desc
    </select>
    <select id="findAll" resultType="EnterpriseShown">
        select * from enterprise_shown order by id desc
    </select>
    <select id="findOne" resultType="EnterpriseShown">
        select * from enterprise_shown
        where id=#{id}
    </select>
    <update id="delete">
        update enterprise_shown set delete_flg=1  where id=#{id}
    </update>
</mapper>