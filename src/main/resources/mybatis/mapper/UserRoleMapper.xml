<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jones.mars.repository.UserRoleMapper" >
    <insert id="insertRecord" parameterType="UserRoles" useGeneratedKeys="true" keyProperty="id">
            insert into user_role(`id`,`role_id`, `user_id`, `create_time`, `update_time`)
            values
            <foreach collection="userRoleList" item="param" index="index" separator=",">
            (#{param.id}, #{param.roleId}, #{param.userId}, now(), now())
            </foreach>
            ON DUPLICATE KEY UPDATE update_time=now()
    </insert>
    <select id="findList" parameterType="UserRoleQuery" resultType="UserRole">
        <include refid="com.jones.mars.repository.CommonMapper.startPage" />
        select * from user_role where 1=1
        <if test="roleId != null">
            and role_id=#{roleId}
        </if>
        order by id desc
        <include refid="com.jones.mars.repository.CommonMapper.endPage" />
    </select>
    <select id="findCount" resultType="java.lang.Integer" parameterType="Query">
        select count(*) from user_role where 1=1
        <if test="roleId != null">
            and role_id=#{roleId}
        </if>
    </select>
    <select id="findAll" parameterType="UserRoleQuery" resultType="UserRole">
        select * from user_role where 1=1
        <if test="roleId != null">
            and role_id=#{roleId}
        </if>
    </select>
    <delete id="delete">
        delete from user_role where role_id=#{roleId}
        <if test="userId != null">
            and user_id=#{userId}
        </if>
    </delete>
    <delete id="deleteByEnterpriseUser">
        delete from user_role where role_id in (select id from role where enterprise_id=#{enterpriseId})
        <if test="userId != null">
            and user_id=#{userId}
        </if>
    </delete>
</mapper>